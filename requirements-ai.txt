# Heavy AI, YOLO, PyTorch & OCR requirements for local GPU video stream processing
# (Install locally with: pip install -r requirements-ai.txt)
-r requirements.txt
opencv-python==4.10.0.84
ultralytics>=8.3.0
# torch & torchvision (install with your CUDA version if GPU available)

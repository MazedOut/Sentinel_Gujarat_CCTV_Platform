# ============================================================
# Milestone 1 — core stream ingestion
# ============================================================

# --- HTTP client (catalogue fetch) ---
httpx==0.27.2

# --- Video / stream ingestion ---
opencv-python==4.10.0.84

# --- Data validation ---
pydantic==2.9.2
pydantic-settings>=2.5.2

# --- Environment / config ---
python-dotenv==1.0.1

# --- Async + utilities ---
aiohttp==3.12.15

# ============================================================
# Milestone 2 — vehicle detection (YOLOv8 + GPU)
# ============================================================

# YOLOv8 — auto-downloads model weights on first use (~6-22 MB)
ultralytics>=8.3.0

# PyTorch with CUDA support (RTX 4060 — CUDA 12.x)
# Install via: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# Listed here for documentation — the --index-url flag cannot be in requirements.txt
# torch>=2.4.0
# torchvision>=0.19.0

# ============================================================
# Future milestones will add:
# ============================================================
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.28
psycopg2-binary>=2.9.9
geoalchemy2>=0.14.7
alembic>=1.13.1

# paddleocr paddlepaddle       <- ANPR OCR (Milestone 3)
# passlib[bcrypt] python-jose  <- Auth/JWT (Milestone 5)
